name: Greeting on variable day

on:
  workflow_dispatch

env:
  DAY_OF_WEEK: Monday

jobs:
  greeting_job:
    runs-on: windows-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello Mona it's Monday"
        run: echo "$Greeting $First_Name. Today is $day_Of_WEEK!"
        run: echo "The config variable is: ${{ vars.day_of_WEEK }} - $firstNAME
        env:
          First_Name: Mona
